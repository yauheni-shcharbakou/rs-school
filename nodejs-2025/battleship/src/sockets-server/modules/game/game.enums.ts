export enum GameBoardPositionStatus {
  PLACED,
  MISS,
  HIT,
}

export enum GameShipType {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
  HUGE = 'huge',
}

export enum GameShipAttackFeedback {
  MISS = 'miss',
  KILLED = 'killed',
  SHOT = 'shot',
}
